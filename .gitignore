# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code
.vscode/

# Flutter/Dart/Pub
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds

# Web
lib/generated_plugin_registrant.dart

# Environment / secrets — never commit real Supabase keys.
.env
.env.local
*.env
!.env.example

# Supabase local
supabase/.branches
supabase/.temp

# graphify — knowledge graph output.
# graph.json / GRAPH_REPORT.md / graph.html are committable if the team wants a
# shared map (see README "Team setup"); the cache and cost ledger are local.
graphify-out/cache/
graphify-out/cost.json
graphify-out/.graphify_analysis.json
graphify-out/.graphify_python
# absolute path to the machine that built the graph — never portable
graphify-out/.graphify_root
